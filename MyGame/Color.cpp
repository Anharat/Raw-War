// Color.cpp

#include "Color.h"

Color::Color(int r, int g, int b, int a)
    : r(r), g(g), b(b), a(a) {
    // Constructor implementation
    // Set the color components (red, green, blue, alpha) using the provided values
}
