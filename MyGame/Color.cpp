// Color.cpp

#include "Color.h"  // Include the Color header file

// Constructor implementation
Color::Color(int r, int g, int b, int a)
    : r(r), g(g), b(b), a(a) {  // Initialize color components with the provided values
    // No additional code needed here as the color components are already set
}