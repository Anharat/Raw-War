// FiringPattern.h

#pragma once  // Preprocessor directive to prevent multiple inclusions of the header file

// Enum class for different firing patterns
enum class FiringPattern {
    NoFiring,       // No firing
    KeyboardFiring, // Firing is controlled by keyboard input
    SteadyFiring    // Steady firing
};